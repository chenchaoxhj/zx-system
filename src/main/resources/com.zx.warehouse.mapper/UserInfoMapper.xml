<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zx.warehouse.mapper.UserInfoMapper">
    <select id="getUserInfo" parameterType="java.lang.String" resultType="com.zx.warehouse.pojo.UserInfoModel">
        select * from UserInfo
        <trim prefix="where" prefixOverrides="and">
            <if test="id != null and id != ''">
                and id=#{id}
            </if>
        </trim>
    </select>


    <select id="getLoginUser" parameterType="java.lang.String" resultType="java.lang.Integer">
        select count(*) from user_login where user_name=#{username} and login_password=#{password}
    </select>
</mapper>
